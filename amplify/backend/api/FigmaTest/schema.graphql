type Question @model @auth(rules: [{allow: public}]) {
  id: ID!
  Question: String
  Category: String
  Points: Int
  Answer: String
}

type Test @model @auth(rules: [{allow: public}]) {
  id: ID!
  address: String
  price: Float
  image_url: String
}
 